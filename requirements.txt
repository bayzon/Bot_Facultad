# Bot Facultad — RAG + WhatsApp Drive (pinned)
# Generated PR1 Foundation — keep llama-index/chroma/openai pinned as per spec
# Install: pip install -r requirements.txt

# Core RAG (pinned per task 1.1)
llama-index==0.14.24
llama-index-core==0.14.24
llama-index-embeddings-openai==0.6.0
llama-index-llms-openai==0.7.10
llama-index-readers-file==0.7.0
llama-index-vector-stores-chroma==0.6.0
chromadb==1.5.9
openai==2.54.0

# Web / WhatsApp / Drive (new for whatsapp-drive-materias)
fastapi==0.116.1
uvicorn==0.52.4
httpx==0.28.1
google-api-python-client==2.143.0
google-auth==2.41.1

# Config / files
python-dotenv==1.2.3
pypdf==6.18.0
python-docx==1.1.2
docx2txt==0.8

# Gemini free-tier (dual provider, preferred when GEMINI_API_KEY is set)
google-generativeai==0.8.6
llama-index-llms-gemini==0.6.2
llama-index-embeddings-gemini==0.4.2

# Transitive / keep aligned with env freeze
pydantic==2.13.5
pydantic-settings==2.15.0
# Google transitives are resolver-owned (resolved from direct pins above).
pillow>=10.4.0

# Tests (needed for tests/ suite)
pytest==9.1.1
